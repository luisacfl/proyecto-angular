import { FiltrarAlumPipe } from './filtrar-alum.pipe';

describe('FiltrarAlumPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarAlumPipe();
    expect(pipe).toBeTruthy();
  });
});
